Cnhjrf// выгрузить результат в ВТ (поставить галочку в пакете результата запроса)
// зайти в элемент результата запроса
// в открывшемся окне, есть поле "модуль"
// у поля модуль, есть кнопка "шаблоны" 

Для каждого Строка из Результат Цикл

	СправочникОбъект = Строка.Ссылка.ПолучитьОбъект();
	//СправочникОбъект.ПометкаУдаления = Истина;
	СправочникОбъект.Записать();
	// СправочникОбъект.Удалить();
	// СправочникОбъект.Провести(); // Для документов

КонецЦикла;